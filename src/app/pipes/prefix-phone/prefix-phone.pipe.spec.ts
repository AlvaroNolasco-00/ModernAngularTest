import { PrefixPhonePipe } from './prefix-phone.pipe';

describe('PrefixPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
